# kyuuyo プロジェクトについて

給与計算・給与仕様に関するプロジェクトです。

## 主なファイル

- `給与仕様.xlsx` — 給与計算の仕様・フォーマットを定義したExcelファイル

## Git運用ルール

- **コードやファイルを変更するたびに、変更内容をコミットしてGitHubにプッシュすること。**
  - 変更 → `git add` → `git commit` → `git push` を1セットとして、変更が完結するたびに実行する。
  - 作業を溜め込んで後でまとめてプッシュすることは避ける。
- リモートリポジトリ: https://github.com/hiro0112/kyuuyo.git
- デフォルトブランチ（`main`）に対して直接コミット・プッシュする運用とする（特に指示がない限りブランチは分けない）。
- コミットメッセージは変更内容が分かるように、日本語または英語で簡潔に記述する。
- force push（`git push --force`）や履歴を書き換える操作は行わない。
